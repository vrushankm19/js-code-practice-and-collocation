// Param Destructuring

// Object
// React in Use Mostly

const a = { // object [1]
    fname: "Vrushank",
    sex: "Male",
}

function r(t) { // Object Destruccring [3]
    console.log(t.fname);
    console.log(t.sex);
}

let s = r(a); // Pass Object as a Peramiter [2]