// Important Array Methods

let a = [1, 2, 3, 4, 5, 6, 7, 8, 9];

function rr(arr, ind) {
    console.log(`Index of this values ${ind}`); // index Of Values
    console.log(`${arr}*2 = ${arr*2}`); // array value multipcation
}

rr(a[8], 5); // Array Index values return in Function